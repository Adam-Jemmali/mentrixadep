/** Default Next/Image quality — sharp enough for UI, light on bandwidth. */
export const IMAGE_QUALITY = 70;

/** Small icons (≤48px display). */
export const IMAGE_QUALITY_ICON = 65;

/** Hero / above-the-fold marketing shots. */
export const IMAGE_QUALITY_HERO = 75;
