"use client";

/**
 * @deprecated Click sounds live in `root-layout-client.tsx` (`useGlobalClickSounds`).
 * This stub remains so stale dev chunks that still lazy-load this path do not 404.
 */
export function ClickSoundProvider() {
  return null;
}
