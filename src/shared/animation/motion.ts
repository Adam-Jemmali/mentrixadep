/**
 * Single entry for Motion.dev / Framer Motion React bindings.
 * Micro-interactions: whileHover, whileTap, layout.
 * Exits and layout groups: AnimatePresence, LayoutGroup.
 */
export {
  motion,
  m,
  AnimatePresence,
  LayoutGroup,
  useReducedMotion,
  useAnimationControls,
  useMotionValue,
  useTransform,
  useSpring,
  animate,
} from "motion/react";

export type { MotionProps, Variants, Transition } from "motion/react";
