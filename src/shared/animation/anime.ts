/**
 * Single entry for Anime.js — import from here, never from "animejs" in feature code.
 */
export {
  animate,
  createTimeline,
  createTimer,
  stagger,
  spring,
  utils,
} from "animejs";

export type { JSAnimation, Timeline } from "animejs";
