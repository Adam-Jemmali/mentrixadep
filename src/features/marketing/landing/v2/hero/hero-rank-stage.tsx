"use client";

import { HeroRankOrbitGame } from "@/features/marketing/landing/v2/hero/hero-rank-orbit-game";

/** Interactive rank orbit mini game for the landing hero. */
export function HeroRankStage() {
  return <HeroRankOrbitGame />;
}
