/** Navbar + hero headline typewriter — keep in sync */
export const BRAND_TYPEWRITER = {
  speed: 100,
  waitTime: 10000,
  cursorChar: "_" as const,
  cursorClassName: "text-purple-500",
} as const;
