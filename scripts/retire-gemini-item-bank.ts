#!/usr/bin/env npx tsx
/**
 * Retired. Gemini must not generate or auto-approve item bank questions.
 * Use reviewed pack import instead.
 */

console.error("Retired: Gemini item-bank generation is disabled.");
console.error("Use: npm run item-bank:import -- scripts/data/item-packs/<pack>.json");
console.error("Exceptions stay in /admin/item-review.");
process.exit(1);
