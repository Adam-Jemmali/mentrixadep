# @react-three/drei@10 declares peer react@^19 while this app stays on React 18 (Next 14).
legacy-peer-deps=true

# Quieter installs (does not remove transitive deprecation notices from upstream packages).
fund=false
progress=false

# Default audit exit behavior (fail on low+) — keep dependency tree aligned with `npm audit` clean.
